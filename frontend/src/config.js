const PORT = 4000;
export const SIMPLE_URL = `http://localhost:${PORT}/`;
export const POST_URL = `http://localhost:${PORT}/post`;
export const PROFILE_URL = `http://localhost:${PORT}/profile`;
export const LOGOUT_URL = `http://localhost:${PORT}/logout`;
export const LOGIN_URL = `http://localhost:${PORT}/login`;
export const REGISTER_URL = `http://localhost:${PORT}/register`;
